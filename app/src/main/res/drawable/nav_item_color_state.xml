<!-- res/drawable/nav_item_color_state.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected state -->
    <item android:color="@color/green_dark" android:state_checked="true" />
    <!-- Pressed state -->
    <item android:color="@color/green_dark" android:state_pressed="true" />
    <!-- Default/inactive -->
    <item android:color="@color/green_dark" />
</selector>
